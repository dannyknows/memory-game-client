import React, { useState, Component } from "react";

function visArray(title, setState) {
  console.log(setState)
  // setState()
  // setState((s) => {
  //   if (s.visible.length === 2) {
  //     s.visible.shift();
  //     s.visible.push(tileId);
  // //   } else {
  // //     s.visible.push(tileId);
  //   }
  // });
  // console.log(this.state.visible);
  // return;
}

export default visArray;
